name: Deploy to Raspberry Pi

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allow manual triggering

jobs:
  test:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4

  deploy:
    needs: test
    runs-on: self-hosted
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Python dependencies (system)
      run: |
        cd ${{ secrets.PI_BOT_PATH || '/home/skula/Sir-Tim-the-Timely' }}
        python3 -m pip install --upgrade pip
        python3 -m pip install -r requirements.txt --break-system-packages
    
    - name: Start Sir Tim bot in screen session
      run: |
        cd ${{ secrets.PI_BOT_PATH || '/home/skula/Sir-Tim-the-Timely' }}
        screen -S sir-tim-bot -X quit || true
        screen -dmS sir-tim-bot python3 main.py
        echo "Sir Tim bot started in screen session"

    - name: Show Logs
      run: |
        cd ${{ secrets.PI_BOT_PATH || '/home/skula/Sir-Tim-the-Timely' }}
        tail -n 50 bot.log
